#ifndef ENUMERATION_H
#define ENUMERATION_H

enum class Direction {backward = 0, forward = 1};

enum class MovingMode {fixedLength = true, constantSpeed = false};

enum class Wheel{left = 0, right = 1};

#endif // ENUMERATION_H
